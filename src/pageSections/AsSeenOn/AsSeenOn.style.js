import styled from 'styled-components'

const StyledAsSeenOn = styled.div.attrs({
  className:
    'bg-white flex flex-column items-center justify-between ph4 ph6-ns page-padding montserrat w-100',
})`
`
export default StyledAsSeenOn
