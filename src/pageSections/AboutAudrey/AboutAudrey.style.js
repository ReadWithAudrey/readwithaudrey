import styled from 'styled-components'

const About = styled.div.attrs({
  className:
    'flex flex-column items-center justify-between pt4 pb5 ph4 montserrat w-100',
})`
  background-color: var(--light-green);
`
export default About
